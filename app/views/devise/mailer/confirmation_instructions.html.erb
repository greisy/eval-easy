<p>¡Bienvenido(a) a EvalEasy, <%= "#{@resource.name} #{@resource.last_name} (#{@email})" %>!</p>

<p>Puedes confirmar tu cuenta de correo a través del siguiente link:</p>

<p><%= link_to 'Confirmar mi cuenta', confirmation_url(@resource, confirmation_token: @token) %></p>
